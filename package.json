{
  "name": "@foldrun/core",
  "version": "0.1.0",
  "description": "Agents are just folders. The format, the runner, the checks \u2014 run agents written as folders of markdown.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./git-hook": "./dist/src/git-hook.js",
    "./*": {
      "types": "./dist/src/*.d.ts",
      "default": "./dist/src/*.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build",
    "test": "FOLDRUN_DATA=$(mktemp -d /tmp/foldrun-test-XXXXXX) node --test *.test.ts $(ls tests/*.test.ts | grep -v e2e)",
    "container": "FOLDRUN_CONTAINER_E2E=1 node scripts/ts-test.mjs tests/container-e2e.test.ts --test-timeout=600000"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.220",
    "gray-matter": "^4.0.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/foldrun-io/foldrun-core.git"
  },
  "homepage": "https://foldrun.io",
  "bugs": {
    "url": "https://github.com/foldrun-io/foldrun-core/issues"
  },
  "keywords": [
    "agents",
    "llm",
    "markdown",
    "workflows",
    "claude"
  ],
  "publishConfig": {
    "access": "public"
  }
}

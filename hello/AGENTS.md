---
name: hello
description: A runnable demo — one agent, one flow, no network and no secrets.
mdagent_version: "0.1"
---

# hello

Everything here is deliberately small enough to run in one step. There are no
API keys, no network calls and no scripts: the point is to prove the loop —
an agent reads what it was given, does the work, and writes back what it
learned as a conformant OKF document.
